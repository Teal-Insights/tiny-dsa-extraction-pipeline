Keep the scenario faithful to the original narrative. Express each step with {api_import_path} using the same interaction model as canonical_api_usage from {canonical_api_example_path}. Split the workflow into several short runnable cells that call keyword-only compute_* helpers with explicit keyword arguments. Do not create an evaluation context or call make_context() / set_*.
