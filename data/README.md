# Workbook inputs

Place the source workbook at `workbook.xlsx` and the human guide at `guide.md`.

After a passing Excel parity sweep, commit reference reports under `differential/exported_library/`.
