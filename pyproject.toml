[project]
name = "tiny-dsa-extraction-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "excel-grapher",
]

[tool.uv.sources]
excel-grapher = { git = "https://github.com/Teal-Insights/excel-grapher.git" }

[dependency-groups]
dev = [
    "fastpyxl>=1.0.9",
    "pywin32>=311; sys_platform == 'win32'",
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
    "quarto>=0.1.0",
    "ruff>=0.15.12",
    "ty>=0.0.34",
    "xlwings>=0.35.3",
    "python-dotenv>=1.2.2",
    "openai>=2.36.0",
    "vl-convert-python>=1.9.0.post1",
    "matplotlib>=3.10.9",
    "pydantic>=2.13.4",
]

[tool.pytest.ini_options]
addopts = "-v -p no:faulthandler -p no:warnings -p no:cacheprovider"
