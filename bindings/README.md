# Series bindings

Author `inputs.bindings.yaml` and `outputs.bindings.yaml` here before running the pipeline.

Use schema version `1.5.0` and the prompt in [templates/binding-authoring-prompt.txt](../templates/binding-authoring-prompt.txt).
