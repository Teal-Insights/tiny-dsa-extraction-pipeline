[project]
name = "extraction-pipeline-docs"
version = "0.1.0"
description = "Great Docs site for the graph-first extraction pipeline"
requires-python = ">=3.13"
dependencies = [
    "great-docs",
    "quarto",
]

[tool.uv]
package = false
